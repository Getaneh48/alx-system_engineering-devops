/**
* mul - checks for a digit (0 through 9).
*
* @a: first integer argument
* @b: second integer argument
*
* Return: result of a multiplication of two numbers
*/
int mul(int a, int b)
{

	int result;

	result = a * b;

	return (result);
}
